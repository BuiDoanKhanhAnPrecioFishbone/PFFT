---
tools:
  - codebase
  - editFiles
  - runCommands
  - problems
  - new
description: Implement Phase 2 — Design System Aware (Figma Variables, component variants, Supabase registry, batch processing)
---

Use the `phase-orchestrator` prompt with phase set to `2`.
