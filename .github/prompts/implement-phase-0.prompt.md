---
tools:
  - codebase
  - editFiles
  - runCommands
  - problems
  - new
description: Implement Phase 0 — Foundation & Prototype (plugin scaffold, two-pass Claude pipeline, font normalization, Auto Layout mapping)
---

Use the `phase-orchestrator` prompt with phase set to `0`.
